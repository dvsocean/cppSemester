#include<iostream>
#include<cmath>

using namespace std;

int main(){

    float PI = 3.14159;

    int s = sqrt(12);
    int sine = sin(2 * PI);

    char x = 'H';// double qoutes implies a string therefore it will not compile

    cout << x;

    return 0;
}